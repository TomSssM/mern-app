import { PORT } from './app';
import { MONGO_URI } from './db';
import { ROOT_PATH, BUILD_PATH, STATIC_PATH } from './path';

export default {
  PORT,
  ROOT_PATH,
  BUILD_PATH,
  STATIC_PATH,
  MONGO_URI,
};
